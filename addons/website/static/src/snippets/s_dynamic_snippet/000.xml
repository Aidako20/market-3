<?xmlversion="1.0"encoding="UTF-8"?>
<templatesxml:space="preserve">
    <tt-name="website.s_dynamic_snippet.grid">
        <!--Content-->
        <tt-set="colClass"t-value="'col-'+(12/chunkSize).toString()"/>
        <tt-set="rowIndexGenerator"t-value="Array.from(Array(Math.ceil(data.length/chunkSize)).keys())"/>
        <tt-set="colIndexGenerator"t-value="Array.from(Array(chunkSize).keys())"/>
        <tt-foreach="rowIndexGenerator"t-as="rowIndex">
            <divclass="rowmy-4">
                <tt-foreach="colIndexGenerator"t-as="colIndex">
                    <tt-if="(rowIndex*chunkSize+colIndex)&lt;data.length">
                        <divt-attf-class="#{colClass}">
                            <tt-raw="data[rowIndex*chunkSize+colIndex]"/>
                        </div>
                    </t>
                </t>
            </div>
        </t>
    </t>
</templates>
