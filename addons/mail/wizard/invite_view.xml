<?xmlversion="1.0"encoding="utf-8"?>
<flectra>
    <data>

        <!--wizardview-->
        <recordmodel="ir.ui.view"id="mail_wizard_invite_form">
            <fieldname="name">AddFollowers</field>
            <fieldname="model">mail.wizard.invite</field>
            <fieldname="arch"type="xml">
                <formstring="AddFollowers">
                    <group>
                        <fieldname="res_model"invisible="1"/>
                        <fieldname="res_id"invisible="1"/>
                        <fieldname="partner_ids"widget="many2many_tags_email"
                                placeholder="Addcontactstonotify..."
                                context="{'force_email':True,'show_email':True}"
                                invisible="context.get('mail_invite_follower_channel_only')"/>
                        <fieldname="channel_ids"widget="many2many_tags"
                                placeholder="Addchannelstonotify..."
                                invisible="notcontext.get('mail_invite_follower_channel_only')"
                                options="{'no_create':True}"/>
                        <fieldname="send_mail"invisible="context.get('mail_invite_follower_channel_only')"/>
                        <fieldname="message"attrs="{'invisible':[('send_mail','!=',True)]}"options="{'style-inline':true,'no-attachment':true}"class="test_message"/>
                    </group>
                    <footer>
                        <buttonstring="AddFollowers"
                            name="add_followers"type="object"class="btn-primary"/>
                        <buttonstring="Cancel"class="btn-secondary"special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</flectra>
