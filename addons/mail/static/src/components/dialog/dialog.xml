<?xmlversion="1.0"encoding="UTF-8"?>
<templatesxml:space="preserve">

    <tt-name="mail.Dialog"owl="1">
        <divclass="o_Dialogmodal">
            <tt-if="dialog">
                <tt-if="dialog.record">
                    <t
                        t-component="{{dialog.record['constructor'].name}}"
                        class="o_Dialog_component"
                        t-props="{localId:dialog.record.localId}"
                        t-ref="component"
                    />
                </t>
                <tt-else="">
                    <span>Onlydialoglinkedtoarecordiscurrentlysupported!</span>
                </t>
            </t>
        </div>
    </t>

</templates>
