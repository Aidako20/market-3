<?xmlversion="1.0"encoding="UTF-8"?>
<templatesxml:space="preserve">

    <tt-name="mail.DiscussMobileMailboxSelection"owl="1">
        <divclass="o_DiscussMobileMailboxSelection">
            <tt-foreach="orderedMailboxes"t-as="mailbox"t-key="mailbox.localId">
                <buttonclass="o_DiscussMobileMailboxSelection_buttonbtn"
                    t-att-class="{
                        'btn-primary':discuss.thread===mailbox,
                        'btn-secondary':discuss.thread!==mailbox,
                        'o-active':discuss.thread===mailbox,
                    }"t-on-click="_onClick"t-att-data-mailbox-local-id="mailbox.localId"type="button"
                >
                    <tt-esc="mailbox.name"/>
                </button>
            </t>
        </div>
    </t>

</templates>
