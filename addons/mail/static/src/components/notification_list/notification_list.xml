<?xmlversion="1.0"encoding="UTF-8"?>
<templatesxml:space="preserve">

    <tt-name="mail.NotificationList"owl="1">
        <divclass="o_NotificationList"t-att-class="{'o-empty':notifications.length===0}">
            <tt-if="notifications.length===0">
                <divclass="o_NotificationList_noConversation">
                    Noconversationyet...
                </div>
            </t>
            <tt-else="">
                <tt-foreach="notifications"t-as="notification"t-key="notification.uniqueId">
                    <tt-if="notification.type==='thread'andnotification.thread">
                        <ThreadPreview
                            class="o_NotificationList_preview"
                            t-att-class="{'o-mobile':env.messaging.device.isMobile}"
                            threadLocalId="notification.thread.localId"
                        />
                    </t>
                    <tt-if="notification.type==='thread_needaction'andnotification.thread">
                        <ThreadNeedactionPreview
                            class="o_NotificationList_preview"
                            t-att-class="{'o-mobile':env.messaging.device.isMobile}"
                            threadLocalId="notification.thread.localId"
                        />
                    </t>
                    <tt-if="notification.notificationGroup">
                        <NotificationGroup
                            class="o_NotificationList_group"
                            notificationGroupLocalId="notification.notificationGroup.localId"
                        />
                    </t>
                    <tt-if="notification.type==='flectrabotRequest'">
                        <NotificationRequest
                            class="o_NotificationList_notificationRequest"
                            t-att-class="{'o-mobile':env.messaging.device.isMobile}"
                        />
                    </t>
                    <tt-if="!notification_last">
                        <divclass="o_NotificationList_separator"/>
                    </t>
                </t>
            </t>
        </div>
    </t>

</templates>
