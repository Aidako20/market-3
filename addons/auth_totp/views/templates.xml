<flectra>
    <templateid="assets_tests"inherit_id="web.assets_tests">
        <xpathexpr="."position="inside">
            <scripttype="text/javascript"src="/auth_totp/static/tests/totp_flow.js"></script>
        </xpath>
    </template>
    <templateid="auth_totp_form"name="Two-FactorAuthentication">
        <tt-call="web.login_layout">
            <tt-set="disable_footer">1</t>
            <divclass="oe_login_form">
                <h5class="card-title">Two-factorAuthentication</h5>
                <formmethod="POST"action=""class="">
                    <inputtype="hidden"name="csrf_token"t-att-value="request.csrf_token()"/>
                    <inputtype="hidden"name="redirect"t-att-value="redirect"/>
                    <divclass="form-group">
                        <labelfor="totp_token">AuthenticationCode(6digits)</label>
                        <inputid="totp_token"name="totp_token"class="form-controlmb-2"
                               autofocus="autofocus"required="required"/>
                    </div>
                    <pclass="alertalert-danger"t-if="error"role="alert">
                        <tt-esc="error"/>
                    </p>
                    <divclass="mb-2mt-2text-muted">
                        <inputtype="checkbox"name="remember"id="switch-remember"value="1"/>
                        <labelfor="switch-remember">Don'taskagainonthisdevice</label>
                    </div>
                    <divt-attf-class="clearfixoe_login_buttonstext-centermb-1">
                        <buttontype="submit"class="btnbtn-primarybtn-block">
                            Verify
                        </button>
                    </div>
                    <divclass="smallmb-2mt-2text-muted">
                        <iclass="fafa-2xfa-mobilepull-left"/>
                        Openthetwo-factorauthenticationapponyour
                        devicetoobtainacodeandverifyyouridentity
                    </div>
                </form>
            </div>
            <divclass="text-centerpb-2border-top">
                <formmethod="POST"action="/web/session/logout"class="form-inline">
                    <inputtype="hidden"name="csrf_token"t-att-value="request.csrf_token()"/>
                    <buttontype="submit"class="btnbtn-linkbtn-smmb-2">
                        Cancel
                    </button>
                </form>
            </div>
        </t>
    </template>
</flectra>
