<?xmlversion="1.0"encoding="utf-8"?>
<flectra>

<templateid="registration_attendee_details_questions"inherit_id="website_event.registration_attendee_details"name="RegistrationAttendeeDetailswithquestions">
    <!--Attendeespecificquestions-->
    <xpathexpr="//t[@name='attendee_loop']//*[hasclass('modal-body')]"position="inside">
        <tt-if="event.specific_question_ids">
            <divt-foreach="event.specific_question_ids"t-as="question"class="row">
                <divclass="col-lg-6mt-2">
                    <tt-call="website_event_questions.registration_event_question">
                        <tt-set="registration_index"t-value="counter"/>
                    </t>
                </div>
            </div>
        </t>
    </xpath>
    <!--Genericquestions-->
    <xpathexpr="//*[hasclass('modal-footer')]"position="before">
        <tt-if="event.general_question_ids">
            <divclass="modal-bodybg-lightborder-bottomo_wevent_registration_question_global">
                <divt-foreach="event.general_question_ids"t-as="question"class="row">
                    <divclass="col-lg-6mt-2">
                        <tt-call="website_event_questions.registration_event_question">
                            <tt-set="registration_index"t-value="0"/>
                        </t>
                    </div>
                </div>
            </div>
        </t>
    </xpath>
</template>

<templateid="registration_event_question"name="RegistrationEventQuestion">
    <labelt-esc="question.title"/>
    <tt-if="question.question_type=='simple_choice'">
        <selectt-attf-name="question_answer-#{registration_index}-#{question.id}"class="custom-select"required="true">
            <tt-foreach="question.answer_ids"t-as="answer">
                <optiont-esc="answer.name"t-att-value="answer.id"/>
            </t>
        </select>
    </t>
    <tt-elif="question.question_type=='text_box'">
        <textareat-attf-name="question_answer-#{registration_index}-#{question.id}"class="col-lg-12form-control"/>
    </t>
</template>

</flectra>
