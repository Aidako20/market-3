<?xmlversion="1.0"encoding="UTF-8"?>
<templatesid="template"xml:space="preserve">

    <tt-name="point_of_sale.SearchBar"owl="1">
        <divclass="pos-search-bar">
            <divclass="search">
                <spanclass="search-icon"><iclass="fafa-search"></i></span>
                <inputclass="radius-left"t-att-class="{'radius-right':!props.config.filter.show}"
                    t-model="state.searchInput"t-on-keydown="onKeydown"type="text"t-att-placeholder="placeholder"/>
                <ult-if="state.showSearchFieldsandstate.searchInput"class="fields">
                    <tt-foreach="config.searchFields"t-as="value"t-key="value_index">
                        <lit-att-class="{highlight:value_index==state.selectedFieldId}"
                            t-on-click="onClickSearchField(value_index)">
                            <spanclass="field">
                                <tt-esc="value"></t>
                            </span>
                            <span>:</span>
                            <spanclass="term">
                                <tt-esc="state.searchInput"></t>
                            </span>
                        </li>
                    </t>
                </ul>
            </div>
            <divt-if="props.config.filter.show"class="filterradius-right"
                t-on-click.stop="state.showFilterOptions=!state.showFilterOptions">
                <spanclass="down-icon">
                    <iclass="fafa-chevron-down"aria-hidden="true"></i>
                </span>
                <span>
                    <tt-esc="state.selectedFilter"/>
                </span>
                <ult-if="state.showFilterOptions"class="options">
                    <tt-foreach="config.filter.options"t-as="option"t-key="option">
                        <lit-on-click="selectFilter(option)">
                            <tt-esc="option"></t>
                        </li>
                    </t>
                </ul>
            </div>
        </div>
    </t>

</templates>
