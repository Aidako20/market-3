<template>

<tt-name="web.PieChart">
    <graphtype="pie"t-att-title="title">
        <fieldt-att-name="modifiers.groupby.split(':')[0]"t-att-interval="modifiers.groupby.split(':')[1]"/>
        <fieldt-if="modifiers.measure"t-att-name="modifiers.measure"type="measure"/>
    </graph>
</t>

</template>
