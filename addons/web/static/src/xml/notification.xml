<?xmlversion="1.0"encoding="UTF-8"?>
<templatesid="template"xml:space="preserve">

<tt-name="Notification">
    <divt-attf-class="toasto_notification#{widget.className}"
         role="alert"aria-live="assertive"aria-atomic="true">
        <tt-set="closeButton">
            <buttontype="button"
                    class="closeo_notification_close"
                    data-dismiss="toast"aria-label="Close">
                <spanclass="d-inline"aria-hidden="true">×</span>
            </button>
        </t>
        <divt-if="widget.title"class="toast-header">
            <spant-attf-class="fafa-2xmr-3#{widget.icon}o_notification_icon"
                  role="img"t-attf-aria-label="Notification#{widget.name}"
                  t-attf-title="Notification#{widget.name}"/>
            <divclass="d-flexalign-items-centermr-autofont-weight-boldo_notification_title"
                 t-raw="widget.title"/>
            <tt-raw="closeButton"/>
        </div>
        <divt-if="widget.messageorwidget.subtitleorwidget.buttons.length"class="toast-body">
            <tt-if="!widget.title"t-raw="closeButton"/>
            <strongt-if="widget.subtitle"t-raw="widget.subtitle"
                 class="o_notification_subtitle"/>
            <divt-if="widget.message"t-raw="widget.message"
                 class="mr-autoo_notification_content"/>
            <divt-if="widget.buttons.length"class="mt-2o_notification_buttons">
                <buttont-foreach="widget.buttons"t-as="button"type="button"
                        t-attf-class="btnbtn-sm#{button.primary?'btn-primary':'btn-secondary'}">
                    <tt-if="button.icon">
                        <it-if="button.icon.indexOf('fa-')===0"t-attf-class="fafa-fwo_button_icon#{button.icon}"role="img"t-att-aria-label="button.name"t-att-title="button.name"/>
                        <imgt-else=""t-att-src="button.icon"t-att-alt="button.name"/>
                    </t>
                    <spant-esc="button.text"/>
                </button>
            </div>
        </div>
    </div>
</t>

</templates>
