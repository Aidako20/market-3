<?xmlversion="1.0"encoding="utf-8"?>
<flectra>

<templateid="tracks_display_list"inherit_id="website_event_track.tracks_display_list">
    <!--TRACKLIST:ADDREPLAYTAGFORFINISHEDTRACKS-->
    <xpathexpr="//div[@t-foreach='tracks']//div[hasclass('col-md-7')]//span[@t-elif='nottrack.is_track_doneandnottrack.is_track_soon']"position="after">
        <at-elif="track.youtube_video_urland(track.is_publishedoruser_event_manager)"
            t-att-href="track.website_url"class="badgebadge-dangerml-2">Replay
        </a>
    </xpath>
    <!--ADDYOUTUBEICON-->
    <xpathexpr="//div[@t-foreach='tracks']//div[hasclass('col-md-7')]//a/span[@t-field='track.name']"position="before">
        <it-if="track.dateandtrack.youtube_video_urland(track.is_track_soonortrack.is_track_liveortrack.is_youtube_replay)"
            class="fafa-youtube-playtext-dangermr-1"/>
    </xpath>
    <xpathexpr="//div[@t-foreach='tracks']//div[hasclass('col-md-7')]//t/span[@t-field='track.name']"position="before">
        <it-if="track.dateandtrack.youtube_video_urland(track.is_track_soonortrack.is_track_liveortrack.is_youtube_replay)"
            class="fafa-youtube-playtext-dangermr-1"/>
    </xpath>
</template>

</flectra>
