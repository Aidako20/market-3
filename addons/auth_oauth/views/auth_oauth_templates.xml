<?xmlversion="1.0"encoding="utf-8"?>
<flectra>
        <templateid="assets_frontend"inherit_id="web.assets_frontend"name="OAuthProvidersAssets">
            <xpathexpr="//link[last()]"position="after">
                <linkrel="stylesheet"type="text/scss"href="/auth_oauth/static/src/scss/auth_oauth.scss"/>
            </xpath>
        </template>

        <templateid="providers"name="OAuthProviders">
            <tt-if="len(providers)&gt;0">
                <emt-attf-class="d-blocktext-centertext-mutedsmallmy-#{len(providers)iflen(providers)&lt;3else3}">-or-</em>
                <divclass="o_auth_oauth_providerslist-groupmt-1mb-1text-left">
                    <at-foreach="providers"t-as="p"class="list-group-itemlist-group-item-actionpy-2"t-att-href="p['auth_link']">
                        <it-att-class="p['css_class']"/>
                        <tt-esc="p['body']"/>
                    </a>
                </div>
            </t>
        </template>

        <templateid="login"inherit_id="web.login"name="OAuthLoginbuttons">
            <xpathexpr="//form"position="before">
                <tt-set="form_small"t-value="True"t-if="len(providers)&gt;2"/>
            </xpath>
            <xpathexpr="//div[hasclass('o_login_auth')]"position="inside">
                <tt-call="auth_oauth.providers"/>
            </xpath>
        </template>

        <templateid="signup"inherit_id="auth_signup.signup"name="OAuthSignupbuttons">
            <xpathexpr="//form"position="before">
                <tt-set="form_small"t-value="True"/>
            </xpath>
            <xpathexpr="//div[hasclass('o_login_auth')]"position="inside">
                <tt-call="auth_oauth.providers"/>
            </xpath>
        </template>

        <templateid="reset_password"inherit_id="auth_signup.reset_password"name="OAuthResetPasswordbuttons">
            <xpathexpr="//div[hasclass('o_login_auth')]"position="inside">
                <tt-call="auth_oauth.providers"/>
            </xpath>
        </template>
</flectra>
