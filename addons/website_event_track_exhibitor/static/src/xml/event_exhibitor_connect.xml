<?xmlversion="1.0"encoding="UTF-8"?>
<templatesxml:space="preserve">

    <tt-name="exhibitor.connect.closed.modal">
        <divclass="o_wesponsor_js_connect_modal_maincontainer">
            <divclass="rowmt-2">
                <tt-if="!widget.sponsorData.event_is_ongoing">
                    <divclass="col-12alertalert-warningtext-center"role="alert"
                        t-if="widget.sponsorData.event_is_done">
                        Event<spant-esc="widget.sponsorData.event_name"class="font-weight-bold"/>isover.

                        <br/>
                        <span>Joinusnexttimetomeet<bt-esc="widget.sponsorData.name"/>!</span>
                    </div>
                    <divclass="col-12alertalert-warningtext-center"role="alert"
                        t-else="">
                        <spant-esc="widget.sponsorData.name"class="font-weight-bold"/>isnotavailablerightnow.<br/>
                        Event<spant-esc="widget.sponsorData.event_name"class="font-weight-bold"/>
                        <spant-if="widget.sponsorData.event_start_today">
                            startsin
                            <spant-esc="widget.sponsorData.event_start_remaining"/>minutes
                        </span>
                        <spanclass="my-0"t-else="">
                            startson<spant-esc="widget.sponsorData.event_date_begin_located"/>
                        </span>
                    </div>
                </t>
                <divclass="col-12alertalert-warningtext-center"role="alert"
                    t-else="">
                    <spant-esc="widget.sponsorData.name"class="font-weight-bold"/>isnotavailablerightnow.<br/>
                    Comebackbetween
                    <strong>
                        <tt-esc="widget.sponsorData.hour_from_str"/>
                        -
                        <tt-esc="widget.sponsorData.hour_to_str"/>
                    </strong>(<spant-esc="widget.sponsorData.event_date_tz"/>)
                    tomeetthem!
                </div>
                <divclass="col-2">
                    <imgclass="img"style="max-width:100%;"
                        t-att-src="widget.sponsorData.website_image_url"
                        t-att-alt="widget.sponsorData.name"/>
                </div>
                <divclass="col-10">
                    <divclass="d-flexalign-items-topmb-3">
                        <divclass="d-flexflex-column">
                            <divclass="d-flexalign-items-center">
                                <h4t-esc="widget.sponsorData.name"class="mb4"/>
                                <spanclass="badgebadge-primaryml-2"
                                    t-esc="widget.sponsorData.sponsor_type_name"/>
                            </div>
                            <spanclass="text-muted"t-if="widget.sponsorData.subtitle"t-esc="widget.sponsorData.subtitle"/>
                            <spant-if="widget.sponsorData.url">
                                <iclass="fafa-homemr-2"/><at-att-href="widget.sponsorData.url"><spant-esc="widget.sponsorData.url"/></a>
                            </span>
                            <spant-if="widget.sponsorData.email">
                                <iclass="fafa-envelopemr-2"/><at-att-mailto="widget.sponsorData.email"><spant-esc="widget.sponsorData.email"/></a>
                            </span>
                            <spant-if="widget.sponsorData.phone">
                                <iclass="fafa-phonemr-2"/><spant-esc="widget.sponsorData.phone"/>
                            </span>
                        </div>
                        <imgt-if="widget.sponsorData.country_flag_url"
                            class="imgml-auto"
                            style="max-height:36px;"
                            t-att-src="widget.sponsorData.country_flag_url"
                            t-att-alt="widget.sponsorData.country_name"/>
                    </div>
                </div>
                <spanclass="col-12"t-if="widget.sponsorData.website_description"t-raw="widget.sponsorData.website_description"/>
            </div>
            <div>
            </div>
        </div>
    </t>

</templates>
